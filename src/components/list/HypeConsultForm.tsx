import { ReactElement } from "react";

interface HypeConsultFormProps {
  close: () => void;
}
const HypeConsultForm = (props: HypeConsultFormProps): ReactElement => {
  props;
  return <div>HypeConsultForm</div>;
};

export default HypeConsultForm;
